let SERVER_PORT = 8080
let WORKERS = 4

[
  server => [
    host => "0.0.0.0",
    port => #{SERVER_PORT},
    workers => #{WORKERS},
    timeout => 30
  ],
  ssl => [
    enabled => true,
    cert_path => "/etc/ssl/cert.pem",
    key_path => "/etc/ssl/key.pem"
  ],
  routes => array(
    "/api/v1",
    "/api/v2",
    "/static"
  ),
  logging => [
    level => "INFO",
    file => "/var/log/app.log",
    max_size => 10485760
  ]
]

